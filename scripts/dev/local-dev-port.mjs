/** Fixed local Vite port (avoids clashes with other projects on :5173). */
export const LOCAL_DEV_PORT = 3200;

/** Playwright webServer port (separate from day-to-day dev). */
export const E2E_DEV_PORT = 4174;
