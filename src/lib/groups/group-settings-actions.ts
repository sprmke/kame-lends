/** Card action buttons: full width on phone, auto width from `lg` up. */
export const groupSettingsActionClass =
  "touch-target min-h-11 w-full lg:w-auto";

/** Stack actions on phone; inline wrap on desktop. */
export const groupSettingsActionsLayoutClass =
  "flex flex-col gap-2 lg:flex-row lg:flex-wrap lg:items-center";
