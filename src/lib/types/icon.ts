/**
 * Icon components from `lucide-svelte` / `@lucide/svelte`.
 * Svelte 5 `Component<>` and Lucide’s exported constructors do not always
 * align across package versions, so call sites use this alias.
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type IconComponent = any;
